# Pending follow-ups

- Polar organization UUID is set: `b6303f05-be1c-4b45-b847-5979667a3d12` (driftwatch-kit) as `DEFAULT_POLAR_ORG_ID`.
- Override still available via `polar-organization-id` / `POLAR_ORGANIZATION_ID` / `polar.organizationId` if needed.
- Do **not** create the git tag from this commit yet — parent will tag `v1` after verifying live Polar checkout + license keys.
